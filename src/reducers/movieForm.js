const initialState = {
  
  title: "",
  genre: "",
  rating: "",
  description: "",
  runtime: 0

  
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}